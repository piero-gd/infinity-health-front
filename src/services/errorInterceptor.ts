// Este archivo queda vacío para no romper referencias
// Su funcionalidad ahora está integrada directamente en api.ts

/**
 * Esta función ya no hace nada, pero se mantiene para no romper referencias
 */
export function handleApiError(error: Error): void {
  // Funcionalidad trasladada a api.ts para centralizar manejo de errores
  console.log('[Legacy Error Interceptor] Esta función ya no se usa');
}
